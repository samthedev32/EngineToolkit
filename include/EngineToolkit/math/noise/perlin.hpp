#pragma once

#include <EngineToolkit/math/matrix/mat.hpp>

namespace EngineToolkit {

// template <uint16_t W, uint16_t H> mat<W, H, vec2> perlin() {
//   mat<W, H, vec2> out;
//   for (uint16_t x = 0; x < W; x++) {
//     for (uint16_t y = 0; y < H; y++) {
//       out[x][y] =
//           vec2((float)(rand() % 1000) / 1000, (float)(rand() % 1000) / 1000)
//               .normalize();
//     }
//   }
// }

} // namespace EngineToolkit
